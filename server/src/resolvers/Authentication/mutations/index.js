import {
    googleLoginMutation,
    loginMutation,
} from './login.mutation';

import {
    registerMutation
} from './register.mutation';

export {
    googleLoginMutation,
    loginMutation,
    registerMutation,
};