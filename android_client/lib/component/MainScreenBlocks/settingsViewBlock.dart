import 'package:flutter/material.dart';

class SettingsViewBlock extends StatelessWidget {
  const SettingsViewBlock({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Settings'),
    );
  }
}